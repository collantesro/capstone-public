@page
@using CCSInventory.Models

@*
    Page description:
    Placeholder index page until the real one is done.
    In Startup.cs, this page is marked as Authorized, therefore requring login before viewing it.
*@

@{
    ViewBag.Title = "Main Menu";
    bool isAdmin = User.FindFirst("UserRole").Value == UserRole.ADMIN.ToString();
}

<div class="container">
    <p>
        To get started, select a menu option from the menu.
    </p>
</div>
