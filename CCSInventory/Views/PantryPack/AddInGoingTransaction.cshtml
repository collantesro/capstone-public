@model CCSInventory.Models.PantryPackTransaction
@using CCSInventory.Models
@{
    ViewData["title"] = "InGoing Transaction";
}

<div class="form-horizontal">

    <h2>New Transaction</h2>
    <div class="row">
        <div class="col-sm-6 col-sm-offset-2">
            <div class="form-group has-error" asp-validation-summary="All"></div>

            <form method="post" asp-action="InGoingTransaction">
                <label asp-for="Qty">Quantity: </label>
                <input class="form-control" asp-for="Qty" />

                <label asp-for="PantryPackTypeID">Pack Type: </label>
                <select asp-for="PantryPackTypeID">
                    @foreach (PantryPackType pt in (List<PantryPackType>)ViewData["types"])
                    {
                        <option value="@pt.PantryPackTypeID">
                            @pt.PantryPackTypeName
                        </option>
                    }
                </select>
                <br />

                <label asp-for="PantryPackTransactionNote">Transaction Note: </label>
                <input class="form-control" asp-for="PantryPackTransactionNote" />

                <br />
                <input type="submit" value="submit" />
            </form>

            <h2>Past out going transactions</h2>
            @await Component.InvokeAsync("PantryPackTransactions", new { inOrOut = 1 })
        </div>
    </div>
</div>